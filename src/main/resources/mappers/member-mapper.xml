<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="memberMapper">

	<select id="selectMemberSoft" parameterType="map" resultType="member">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME, CREATE_DATE, GRADE, POINT
		FROM MEMBER
		WHERE STATUS = 'Y' AND ID = #{} AND PWD = 'Y'
	</select>
</mapper>