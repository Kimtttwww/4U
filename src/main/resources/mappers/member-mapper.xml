<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="memberMapper">

	<select id="selectMemberSoft" parameterType="member" resultType="member">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME, CREATE_DATE
		FROM MEMBER
		WHERE STATUS = 'Y' AND MEMBER_ID = #{memberId} AND MEMBER_PWD = #{memberPwd}
	</select>
</mapper>